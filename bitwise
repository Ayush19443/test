
#include <iostream>
#include <cstdio>
#include <vector>
#define IO ios_base::sync_with_stdio(false); cin.tie(NULL);
#define endl '\n'
using namespace std;

int main(){
    IO;

    vector<int> v;
    int n, ans = 0;
    cin >> n;
    for(int i=0; i<n; i++){
        int no;
        cin >> no;
        v.push_back(no);
        ans = ans ^ no;
    }
    cout << ans << endl;

    return 0;
}
